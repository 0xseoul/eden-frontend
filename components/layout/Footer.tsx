import React from "react";

const styles = {
  section: `w-full flex] fixed bottom-0 left-0 right-0 z-10`,
};
const Footer = () => {
  return <section className={styles.section}>footer</section>;
};

export default Footer;
