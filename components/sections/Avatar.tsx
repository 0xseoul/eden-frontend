import React from "react";

const styles = {
  section: `min-h-screen w-full flex justify-center items-center relative`,
};
const Avatar = () => {
  return <div className={styles.section}>Avatar</div>;
};

export default Avatar;
